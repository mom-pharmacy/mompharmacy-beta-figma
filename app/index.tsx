import { View } from 'react-native';
import GetStart from './Login/getstart';

const Index = () => {
  return (
    <View style={{ flex: 1 }}>
      <GetStart />
    </View>
  );
};

export default Index;
